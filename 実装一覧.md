# MOGCIA Dev Agent 実装一覧

## 実装済み

- Next.js App Router / TypeScript / Tailwind CSS 初期構成
- MOGCIAトンマナ反映
  - `#D5B5B2`
  - `#E9CBC8`
  - `#C79A98`
  - `#F8F4F3`
  - `#F7F3F2`
  - `#1F1F22`
  - `#F3B8C0`
  - `#B97B80`

```

## 現行MVP追加分

- デスクトップ連携基盤
  - `/settings/desktop` で端末登録 / トークン発行 / 端末無効化
  - アクセストークンは発行時のみ表示
  - FirestoreにはSHA-256ハッシュのみ保存
  - `desktopDevices` / `desktopMemos` / `desktopAuditLogs` を利用
  - CLI / MacアプリはFirestoreへ直接接続せず、`/api/desktop/*` のみ利用
- デスクトップAPI
  - `/api/desktop/auth/verify`
  - `/api/desktop/devices`
  - `/api/desktop/devices/[deviceId]/revoke`
  - `/api/desktop/tasks/today`
  - `/api/desktop/tasks`
  - `/api/desktop/companies/search`
  - `/api/desktop/activity-logs`
  - `/api/desktop/memos/parse`
  - `/api/desktop/memos/commit`
  - Bearer token認証
  - 端末ごとのpermission確認
  - 簡易rate limit
  - audit log保存
- MOGCIA CLI
  - `mogcia login`
  - `mogcia today`
  - `mogcia memo`
  - `mogcia task add`
  - `mogcia company search`
  - `mogcia company log`
  - `mogcia open`
  - トークンはmacOS Keychainへ保存
- MacメニューバーアプリMVP
  - SwiftUI / MenuBarExtra
  - 今日のタスク表示
  - Web Homeを開く
  - 設定画面
  - フローティングメモ
  - AIメモ整理
  - 保存前の候補確認
- 常設デスクトップウィンドウMVP
  - Xcode不要のElectron版を `apps/desktop-widget` に追加
  - デスクトップ上に小さな常設ウィンドウとして表示
  - 常に前面表示の切り替え
  - 今日のタスク表示
  - 会社検索
  - メモ入力
  - AIメモ整理
  - 活動ログ / タスク / 会社メモ候補の確認後登録
  - トークンはElectron `safeStorage` 対応環境では暗号化保存
- AIメモ解析
  - OpenAI `OPENAI_MODEL` / `OPENAI_CHAT_MODEL` を利用
  - JSON Schemaで活動ログ / タスク / 会社メモ候補を返す
  - ユーザー確認後にのみ保存
- ユーザー表示名固定
  - `TjDadmBAdVYaPEvG3ppfBLS4HGN2` は `石田 真梨奈`
  - `233XesPbdUcARMRgnmWfG3yobtW2` は `堂本 寛人`
  - `58Sgm29AqddR9HtRJwBJuxQH2Fq1` は `北村 健太郎`
  - タスク / カレンダー / 会社ログ / 商材 / ナレッジ / Home / デスクトップAPI保存名に反映
- カレンダー入力フォーム改善
  - 通知 / 繰り返し入力を削除
  - 右上の通知ボタンを削除
  - 種類を `商談 / 打ち合わせ / 電話 / 訪問 / 社内MTG / 作業時間 / 私用 / その他` に拡張
  - 予定追加時は `予定名 / 種類 / 担当者 / 日付 / 開始時刻 / メモ` を中心に整理
  - 終了時刻は画面入力せず、内部で開始から1時間後に自動設定
  - 場所 / URL / 参加者 / 関連会社 / 関連案件 / 関連会議は詳細欄へ移動
